# Repo Instructions

This repository's fork-maintenance bundle lives in `skills/` with shared reference material in `references/`.

For Claude Code, either:

- Run `./scripts/install.sh claude`, or
- Keep this repo-local copy and install or copy the `skills/` and `references/` directories as needed.

Use the skill when syncing upstream, syncing remotes, creating features, or rebuilding `integration/latest`.
